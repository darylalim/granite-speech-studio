[project]
name = "granite-speech-pipeline"
version = "0.7.0"
description = "Speech-to-text and translation pipeline using IBM Granite models on Apple Silicon via MLX"
requires-python = ">=3.12"
dependencies = [
    "mlx-audio",
    "silero-vad",
    "streamlit",
    "torch",
    "torchaudio",
    "torchcodec>=0.10.0",
    "transformers",
]

[dependency-groups]
dev = [
    "pytest",
    "ty",
    "ruff",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff.lint.isort]
combine-as-imports = true

[tool.ty.environment]
python-version = "3.12"
