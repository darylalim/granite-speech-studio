# CLAUDE.md

## Project Overview

Streamlit application for English speech transcription using IBM Granite Speech 5.0 TurboCTC on Apple Silicon with MLX. VAD-based audio segmentation with timestamps. Transcription only, English only: the model is an encoder-only greedy-CTC recogniser with no prompt and no decoder, so there is no translation, no source-language choice and no keyword biasing.

## Setup

```bash
uv sync
uv run streamlit run streamlit_app.py
```

No system FFmpeg is needed: audio is decoded by PyAV (`av`), whose wheel bundles FFmpeg. It used to go through `torchaudio.load` → `torchcodec`, which `dlopen`s whatever FFmpeg Homebrew has installed — see the `av` entry under Dependencies for why that was retired.

## Commands

- **Lint**: `uv run ruff check .`
- **Format**: `uv run ruff format .`
- **Typecheck**: `uv run ty check`
- **Test**: `uv run pytest`

When working with Python, invoke the relevant `/astral:<skill>` (`/astral:uv`, `/astral:ty`, `/astral:ruff`) for uv, ty, and ruff to ensure best practices are followed.

## Git

- **Conventional commits, with scopes** — `type(scope): imperative subject`, lowercase, no trailing period. Types in use: `feat`, `fix`, `perf`, `refactor`, `chore`, `docs`, `test`; scopes: `ci`, `vad`, `pipeline`, `hooks`, `ui`, `deps`. Not cosmetic — the `release` job pipes `git log --format='* %s (%h)'` straight into the published notes, so a malformed subject ships into a GitHub release rather than into a diff nobody reads. The one subject that must *not* ship is the version bump's, and the job drops it by exact spelling — `sed '/^\* chore: bump version to [^ ]* ([0-9a-f]*)$/d'` over the formatted lines — so a bump commit is always `chore: bump version to <version>` and nothing more. A scoped or `build:`-typed bump lands in the notes again, as every CI-published release's did before the filter (v0.12.0 and v0.13.0 still carry theirs; v0.14.0's was removed by hand with `gh release edit`; v0.11.0's notes were hand-written); and a bump that also did something (`… to 0.2.0 and add project description`, 21b5435) is deliberately *not* filtered either — it surfaces so the change it carried gets listed, where a prefix match would have dropped both. The filter runs over the formatted line rather than through `git log --grep` because `--grep` tests every line of the message: a commit merely quoting the bump subject at the start of a body line would vanish from the notes.
- **Work lands directly on `main`; no PRs, no merge commits** (30/30 commits, zero merges) — which is also why `gh --generate-notes` is unusable in the release job (see its comment in `ci.yml`). Commit when asked; leave `git push` to the user.

## Permissions, Hooks & CI

- `.claude/settings.json` **permissions**: `"deny": ["Edit(uv.lock)"]` — the lock is generated and must change only via uv (`uv add`/`remove`/`lock`/`sync`); a hand-edit desyncs it from `pyproject.toml` and breaks resolution. This replaced a PreToolUse hook that did the same check in bash: the rule needs no subprocess per edit, has no `jq` dependency to fail open on, and also covers file commands Claude Code recognises in Bash (`sed -i`, `cat >`) that the hook's `Edit|Write` matcher missed. It must be spelled `Edit(...)`, not `Write(...)` — path rules are consulted for `Edit(path)`/`Read(path)` only, and an `Edit` rule already covers Write and NotebookEdit; a `Write(uv.lock)` rule would be accepted, never consulted, and warn at startup. Running `uv` itself is unaffected: deny rules don't reach subprocesses that write files themselves.
- `.claude/` hooks: a **Stop hook** (`check-on-stop.sh`) auto-runs `ruff check` + `ty check` + `pytest -q` and blocks turn completion on failure — no need to hand-run them at end of turn (~5.5s: ruff 0.03s, ty 0.95s, pytest 4.6s). Two holes worth knowing, both deliberate: the gate is `git status --porcelain`, so it sees **uncommitted** `.py` changes only and `.py` edits committed earlier in the turn leave a clean tree and go unchecked — hand-run the three there; and `stop_hook_active` caps it at one block per turn, so a second stop goes through with the checks still red. It is a prompt to fix, not a wall. A **PostToolUse hook** (`ruff-on-edit.sh`) auto-`ruff format`s edited `.py` files (format only, not `check --fix`). The two don't overlap by design — format is idempotent and safe after every edit, lint is order-dependent and gated once when the code is stable.
- CI + releases (`.github/workflows/ci.yml`): a `check` job — `uv sync --locked`, `ruff check`, `ruff format --check`, `ty check`, `pytest` — on macOS across Python **3.12 and 3.13**, then a `release` job. **Read the file for the rationale**: its comments already answer why macOS is mandatory rather than preferred, why there is deliberately no `brew install ffmpeg` step any more, why `release` reads the version instead of diffing it, and why the notes come from `git log`. `ruff format --check .` there covers five files, not three: since ruff 0.16.0 the formatter formats ```python fences in Markdown by default, so `CLAUDE.md` and `README.md` are in the run (neither has one today; ```bash fences are ignored) — and neither hook can see a fence, so an unformatted Python fence in either file is caught only in CI. Three things the YAML does *not* say, and a session would otherwise undo:
  - **Bumping `version` in `pyproject.toml` is the entire release action.** Push it to main and CI tags `v<version>`, writes notes from the commit subjects since the last tag — minus any bump commit, filtered by its exact `chore: bump version to <version>` subject (see Git) — and publishes, titled `v<version>` explicitly: `gh release create` stores no title unless given one, and GitHub renders an untitled release as `<tag>: <subject of the tagged commit>`, i.e. the bump subject again, in the repo sidebar and the releases list. Idempotent, so a failed run is fixed by re-running it; but idempotent means *already-released tags are left alone*, so a change to the notes format reaches an already-published release only through `gh release edit`, never by re-running the job. Releases publish outright — add `--draft` to `gh release create` if they should instead wait for a hand-edit.
  - Steps **in the `check` job** are unnamed on purpose — GitHub labels an unnamed step with its own command, so `Run uv run ruff check .` beats any `name:` paraphrase on failure. Don't re-add them. The `release` job's one step keeps its `name:` because its body is a 40-odd-line script, where the auto-label would be the whole thing.
  - Both matrix legs earn their place: 3.12 is the `requires-python` floor and ty's target, 3.13 is what a fresh `uv sync` resolves to — and `fail-fast: false` stops a 3.12 failure from cancelling 3.13 and hiding a 3.13-only break.

## Code Style

- snake_case for functions/variables, PascalCase for classes
- Type annotations on all parameters and returns
- `RuntimeError` for known transcription failures (no custom exception class)
- isort with combine-as-imports (configured in `pyproject.toml`)

## Dependencies

- `mlx-audio` — MLX-based speech model loading and inference (Apple Silicon). Declared as `mlx-audio>=0.5.1` (locked at 0.5.5), and the floor is load-bearing: `granite_speech5_ctc` entered `mlx_audio.stt.utils.MODEL_REMAPPING` in 0.5.1 and is absent from 0.5.0 and the whole 0.4.x line, so an older resolution would pass `uv sync` and then fail inside `load_model` on an unknown `model_type`. The 0.5.x line also made `mlx-lm` an optional extra, which is why `mlx-lm` and `sentencepiece` dropped out of `uv.lock` — nothing in the app needs either
- `mlx` — **not declared in `pyproject.toml`**; arrives transitively via `mlx-audio`, yet `streamlit_app.py` imports it directly — `mlx.core` and `mlx.nn` at module scope for the batched VAD path. `mlx-lm` is **no longer imported anywhere**: the speech model is driven purely through `mlx_audio`'s public `generate()` and has no LLM decoder to sample from. An `mlx-audio` release that stops pulling `mlx` in breaks the app with an `ImportError` at import time rather than a resolution error at `uv sync`; `uv add mlx` is the fix if that ever bites. `import mlx.core` needs no `# ty: ignore` any more: mlx 0.32.0 shipped `py.typed` without `mlx/core/*.pyi`, so ty resolved the package but not the compiled `core` extension and the import carried `# ty: ignore[unresolved-import]`; 0.32.2 ships the stubs again, and ty flags a leftover suppression as `unused-ignore-comment`. With the stubs, `array.item()` is typed `scalar` (`bool | int | float | complex`), so `float(x.item())` is an `invalid-argument-type` — take a scalar array to Python with `float(x)` (`array.__float__`), which is what the two VAD-equivalence tests do. If a future mlx drops the stubs again, the suppression goes back on the import line in `streamlit_app.py` and `tests/test_streamlit_app.py`
- `huggingface_hub` — also **not declared**, transitive via `mlx-audio`, and imported directly only by `tests/test_streamlit_app.py` (`try_to_load_from_cache`, the cache gate on the real-weights smoke test — see Tests). Same category as `mlx`: an upstream release that stops pulling it in fails that one test at run time (the import is function-local), not `uv sync`; `uv add huggingface_hub` is the fix
- `torch` — tensor operations (waveform handling, the PyTorch VAD fallback)
- `av` (PyAV) — audio decoding for the whole pipeline (`_decode_audio`) and the header-only duration read (`audio_duration_seconds`). Declared `av>=18.1.0`. Chosen because its wheel **bundles FFmpeg**: the previous decoder, `torchaudio.load` → `torchcodec`, `dlopen`s the FFmpeg that Homebrew has installed, and that broke twice in one week — Homebrew moved `ffmpeg` to 9.0 while torchcodec 0.15 shipped loaders for 4–8 only, and torchcodec 0.16 added the 9 loader but dropped the `/opt/homebrew/opt/ffmpeg/lib` rpath from its dylibs, so on Apple Silicon it finds no FFmpeg at all (upstream: meta-pytorch/torchcodec #1631, fixed by #1632 after 0.16.0 shipped; the rpath it dropped was the one #1152 added to close #570). Neither is fixable from inside the process — dyld reads its search paths at launch, so `os.environ["DYLD_FALLBACK_LIBRARY_PATH"]` and `ctypes` preloading were both tried and both fail — and the alternatives (a versioned `ffmpeg@8` plus a symlink into Homebrew's `opt/`, or a `DYLD_*` variable on every launch command) are not instructions to give users. A fresh `brew install ffmpeg` produced a crash at import; with PyAV there is nothing to install. Decode is verified bit-exact against the WAV fixture's own PCM (`test_wav_decodes_bit_exactly`), and three review-found regressions are pinned by containers built in-test with PyAV's own encoders — `latin1_tagged_wav` (an ANSI RIFF INFO tag must neither fail the load nor blank the duration), `two_track_mkv` (the `default`-flagged second track wins over the first, for both decode and duration) and `undecodable_mkv` (an mp3 track relabelled as AC-4 raises the readable `no decoder is available` error)
- `torchaudio` — the pure-torch resampler only (`torchaudio.functional.resample`, to `SAMPLE_RATE` after the mono downmix). It neither imports nor depends on `torchcodec` (verified: `torchcodec` is absent from `uv.lock` and from `sys.modules` after `import torchaudio`), which is what lets the decoder change leave the resample numerically identical
- `silero-vad` — Voice Activity Detection; supplies the PyTorch fallback checkpoint (its 16 kHz weights are bit-exact with `MLX_VAD_REPO`). Pinned `silero-vad==6.2.1`, and both sides of the pin are deliberate. The floor: this checkpoint is the other half of the bit-exact pair `MLX_VAD_REVISION` freezes, and a bare `<6.2.2` ceiling could let a future conflict resolve to 6.1 or 5.x, whose weights differ — so the floor is the version the equivalence was verified on. The ceiling: 6.2.2 declares `torchaudio<2.10` — its only torchaudio users are its own `read_audio`/`save_audio`, which the app never calls — and torchaudio 2.9.1's wheel pins `torch==2.9.1`, so inheriting it drags torch from 2.14 back to 2.9.1 (exactly what `uv lock --upgrade` did on 2026-09-21 until the bound went in); it also imports `onnxruntime` at package import (`silero_vad/__init__` → `sequence_vad`) while declaring it only as an extra, so `import silero_vad` fails outright here. The `.jit` checkpoint is byte-identical across 6.2.1 and 6.2.2, so nothing is lost. Move the pin once a release drops the torchaudio bound *and* makes the onnxruntime import lazy. `[tool.uv] override-dependencies` was rejected because it would run 6.2.2 against a torchaudio that upstream declares unsupported, and `!=6.2.2` because a 6.2.3 that keeps the bound would silently downgrade torch again on the next `uv lock --upgrade` — an exact pin fails loudly instead
- `streamlit` — web user interface. Declared `streamlit>=1.51.0` in `dependencies` — `st.space` landed in 1.51.0 and is the newest API the layout uses (horizontal containers and `width=` on `st.container`/`st.button` are 1.48.0; every call the app makes was checked against a scratch 1.51.0 install). It was `>=1.52.0` for `text_alignment=` on `st.title`/`st.markdown`, which the sidebar layout removed — and `streamlit>=1.64.0` in the `dev` group (locked at 1.64.0): the higher floor is the tests', not the app's — `AppTest.session_state` became an iterable dict-like wrapper in 1.64.0 (streamlit#16897) and `tests/test_app_ui.py` iterates it for the single-slot `_duration` check, where on 1.63.0 and below the comprehension raises `KeyError` from the legacy `__getitem__(0)` iteration. uv resolves both groups together, so the split changes nothing in the lock
- `ruff` — linting/formatting (dev)
- `ty` — type checking (dev)
- `pytest` — testing (dev)

## Configuration

`pyproject.toml` — ruff isort (`combine-as-imports`), ty (`python-version = "3.12"`), and pytest (`pythonpath = ["."]`). That last one is load-bearing: there is no `conftest.py` and no `[build-system]`, so the project is never installed as a package and pytest's prepend mode puts `tests/` — not the repo root — on `sys.path`. It is the only reason `from streamlit_app import ...` resolves; don't "clean it up" into a `conftest.py`.

`.streamlit/config.toml` — `[server] maxUploadSize` and nothing else. There is deliberately **no `[theme]` table**: the app renders in Streamlit's built-in light and dark themes, and the settings menu offers System / Light / Dark. Every theme option's `default_val` is `None`, and *unset* is what tells the frontend to apply its own palette — a `[theme]` table spelling out the same values would not be equivalent. A partial re-theme is worse than none: a custom theme that sets only top-level `[theme]` keys — nothing under `[theme.light]` *or* `[theme.dark]` — locks the app to a single mode, and the appearance section does not merely lose entries — it **disappears from the menu entirely**, leaving no way back. Verified by A/B on 1.61.1 and again on 1.64.0 (`--theme.primaryColor` alone → no appearance section, app pinned to light despite a dark system setting; no flag → System/Light/Dark present). The frontend builds that section from the available-themes list and early-returns `[]` at length ≤ 1, which is what a top-level-only theme collapses it to. It keeps the menu as soon as *either* sub-table carries a value — the three entries it then emits are the bundle's `Custom Theme Light` / `Custom Theme Dark` / `Custom Theme Auto`, with the missing mode derived from `[theme]` plus its `base` (`--theme.light.primaryColor` alone → System/Light/Dark present, verified on 1.64.0) — but `test_config_defines_no_custom_theme` demands **both** sub-palettes regardless, because a mode that is only derived is a mode nobody reviewed. (It previously carried an IBM Carbon-inspired sub-palette pair plus radii/link/border overrides.) The explanatory comment sits **above** `[server]`, not under it: TOML scoping is positional, so a theme key added beneath a trailing comment would silently become `server.<key>`.

## Architecture

`streamlit_app.py` — single-file app.

### Functions

- `is_video` — predicate by extension, drives `st.video` vs `st.audio` preview
- `format_timestamp` — formats seconds to `M:SS` or `H:MM:SS`
- `audio_duration_seconds` — returns clip duration from the container headers via `_open_container` (no frame decoded): the best audio stream's (`_best_audio_stream`) `duration * time_base`, else the container's `duration / av.time_base` (microseconds), else `None`; also `None` for unparseable bytes, a container with no audio stream, or a non-positive figure. Used to gate the Run button when VAD is off on long audio (threshold `MAX_VAD_OFF_DURATION_S = 1800`, thirty minutes).
- `silero_vad` — runs Silero VAD on waveform, returns `(start, end)` tuples in seconds. Dispatches between two backends over the **same checkpoint**: `_mlx_vad_spans` (MLX, default) and `_torch_vad_spans` (the `silero-vad` PyPI model, fallback). A `torch.nn.Module` routes straight to PyTorch; anything else takes MLX inside a `try/except` that warns (`RuntimeWarning`) and falls back. **Non-16 kHz audio always goes to PyTorch**: the v6 conversion ships no 8 kHz weights, but `mlx_audio` builds that branch anyway and loads non-strictly, so calling it at 8 kHz runs randomly initialised layers and returns confident nonsense instead of failing (verified: loaded 8k weights have std 0.041 vs the trained 0.951)
- `_supports_mlx_vad` — probe over the `mlx_audio` surface the fast path drives (`vad_16k` + its conv/lstm submodules, `_MLX_VAD_BRANCH_CONFIG_ATTRS`, `model.dtype`, the model-level threshold config, and the private `_probs_to_timestamps`). **This is the only place the app still reaches into `mlx_audio` internals** — the speech model is driven through its public `generate()` alone (see the hoist note under `run_pipeline`) — so it is also the reference pattern for any future probe: checked at load time, with the `try/except` in `silero_vad` covering the signature changes `hasattr` cannot see. **Beyond names it also checks two config *values*:** `chunk_size` and `context_size` must equal `VAD_CHUNK_SAMPLES`/`VAD_CONTEXT_SAMPLES`. The revision pin freezes the repo's `config.json` but *not* `mlx_audio`'s `BranchConfig` defaults, which fill in any field the config omits — so a dependency upgrade can still move the framing. Neither this path nor `mlx_audio`'s own `_probs_to_timestamps` (which hardcodes a 512-sample stride) could serve different framing, and getting it wrong produces plausible garbage rather than an error, so refuse instead
- `_mlx_vad_windows` — one `(context + chunk)` window per 512-sample decision chunk, framed exactly as `mlx_audio`'s streaming loop does but via `mx.as_strided`, so the 64-sample overlaps stay a view rather than a 12%-larger copy. Pads both edges in a single `mx.pad`: padding then concatenating the context would allocate two full copies of the waveform, which on a 500 MB upload is a lot of transient memory for a function whose whole purpose is avoiding a copy
- `_mlx_vad_probabilities` — per-chunk speech probabilities with the encoder batched across chunks. The STFT + conv encoder is stateless per chunk (it only ever sees that chunk's 576-sample window), so it runs on a whole batch at once; only the LSTM is sequential, and `nn.LSTM` unrolls a full `(1, T, 128)` sequence in one graph, so carrying `(hidden, cell)` across batches reproduces chunk-by-chunk stepping exactly. One model call per 32 ms of audio becomes one per ~65s. **Measured 2.9–3.5× faster than PyTorch** across 10s–15min, with max |Δp| of 2.5e-06 over 9375 chunks, zero decisions flipped at threshold, and byte-identical spans. Note `mlx_audio`'s own `get_speech_timestamps` is *slower* than PyTorch here (0.66×) — at 309K parameters the per-call dispatch cost dominates the arithmetic, so call count is the only lever, and that is the whole reason this exists rather than using the library method. Guards its own core assumption: the batch axis is reused as the LSTM's sequence axis, which holds **only** while the conv stack collapses each window to one frame. Different STFT framing leaves more (`hop_length=64` measured at 2), and `x[:, 0, :]` would silently drop the rest where the reference averages them — so a frame count other than 1 raises and the caller falls back. A 256 ms noisy-OR "unified" mode (the v6 repo's `example_256ms.py`) was evaluated and rejected: same speed as batching (0.96–1.13×) but it flips 10.1% of chunk decisions, shifts every boundary up to 320 ms, and inflates detected speech ~13% — noisy-OR is one-sided by construction, so the error is systematic, not noise
- `_split_long_segment` — splits an over-long span into equal parts each within the cap (equal parts, not cap-sized chunks plus a remainder, so with a 10s cap a 30.1s span becomes 3 × ~10s rather than 2 × 15s + a 0.1s sliver that would transcribe to nothing). Backs the part count off rather than emit parts under `MIN_SEGMENT_DURATION_S` (20s): every forced split lands mid-utterance and costs a little accuracy (the WER series is under Languages), so a span only modestly over the 30s cap is left whole rather than halved — buffering pushes a natural 30.0s span to 30.6s, which would otherwise become 2 × 15.3s. With the floor above half the cap, spans under 2 × floor (40s) stay one part and only longer ones split. The floor is clamped to `max_duration` so a caller passing a cap below it still gets a split
- `get_speech_segments` — post-processes VAD output with buffering, merging, and a `max_segment_duration` cap (default `MAX_SEGMENT_DURATION_S`, 30s). Merging across sub-`min_gap` gaps stops at the cap, and any span still over it — an unbroken VAD span, or the no-speech full-audio fallback — is split by `_split_long_segment`. Without the cap, continuous speech (podcast, lecture, dictation) merged into one unbounded segment — which this model would transcribe fine (see Languages), but as a single timestamp line spanning the whole clip; the cap is what keeps timestamps readable. When the cap **refuses** a merge, the new segment's start is clamped to the previous segment's end (and skipped entirely if wholly covered): the 0.3s start/end buffers make consecutive spans overlap by `0.6 - gap` — up to 0.6s when the VAD spans abut — and merging used to absorb that unconditionally because a negative gap always satisfies the `min_gap` test. (`min_gap = 0.5` is a different quantity: it gates the merge on the *already buffered* gap, so the raw gap it admits is 1.1s.) The cap is what first made the overlap reachable — unclamped it re-transcribed up to 0.6s at every boundary and emitted timestamps that ran backwards
- `_spans_in_seconds` — shared sample-offset → seconds tail for both backends, so the two paths cannot drift apart on a conversion detail that would break their interchangeability
- `_torch_vad_model` — `@st.cache_resource`-wrapped `load_silero_vad()`, used by every fallback site. `load_silero_vad` is **not** memoised (it re-runs `torch.jit.load` per call), so calling it inline would re-deserialise the model on every pipeline run of a session that permanently falls back, and leave a second copy resident alongside whatever `load_vad_model` cached. That `torch.jit.load` is the app's only TorchScript call, and on torch ≥ 2.14 it emits `FutureWarning: torch.jit.load is deprecated. Please switch to torch.export.` (2.10–2.13 emitted the same text as a `DeprecationWarning`, which pytest already displayed — the 2 warnings in `pytest -q` are the two tests that load the PyTorch checkpoint: the VAD-on UI run under `_offline_mlx_vad()`, and `test_both_backends_agree_on_real_audio`, which calls `load_silero_vad()` directly to compare the backends; the Streamlit console shows it only when the MLX VAD load fails, since the MLX path never touches `torch.jit`). No torch release names a removal version (pytorch/pytorch#189914 calls the deprecation-to-error step paused). Don't filter it: the checkpoint is `silero-vad`'s `.jit` file, so there is nothing here to migrate — if TorchScript loading ever goes, the fallback needs another loader — the wheel ships no non-JIT torch path (its `silero_vad_16k.safetensors` serves only an unwired tinygrad module), though the MLX repo's weights are bit-exact with the `.jit` ones
- `load_vad_model` — cached VAD loader. Loads `MLX_VAD_REPO` and degrades to `_torch_vad_model()` — never `load_silero_vad()` inline, for the memoisation reason above — on either failure mode — the repo not resolving (offline, renamed) or `_supports_mlx_vad` rejecting what came back — warning in both cases, since the PyTorch path is correct but ~3× slower and a silent downgrade would be invisible until someone benchmarked it. `MLX_VAD_REVISION` pins the repo, for a **different reason** than `MODEL_REVISION`. That one guards reproducibility (a retrained checkpoint would still load strictly and decode without complaint — see `load_model`); this repo is org-maintained too, but `mlx_audio`'s VAD loader takes `strict=False` and *cannot* do otherwise — the v6 conversion legitimately ships no `vad_8k.*` keys, so a strict load would reject it outright. Non-strict loading means renamed `vad_16k.*` keys would leave those layers randomly initialised, and the model would still build, still pass `_supports_mlx_vad`, still run clean, and return confident nonsense. That is the same silent-garbage mechanism documented for the 8 kHz branch, and a content hash is the only thing that rules it out. A bad hash is safe: the load 404s, warns, and falls back to PyTorch. Cold start regresses (0.590s vs 0.027s), so on a first run under ~25s of audio the MLX path is net slower; behind `@st.cache_resource` that is once per session
- `load_model` — cached speech-model loader: `mlx_audio.stt.utils.load_model(model_id, revision=revision, strict=True)`, called with `MODEL_ID` and `MODEL_REVISION` (`revision` travels through the loader's `**kwargs`). **`strict=True` is deliberate and the opposite of `mlx_audio`'s default.** `strict=False` leaves any weight the checkpoint fails to supply randomly initialised, and greedy CTC would still decode — into confident nonsense, with no error anywhere. That is exactly the failure mode the VAD loader documents and *cannot* guard against, because its checkpoint legitimately omits the `vad_8k.*` keys; this one is IBM's own bf16 checkpoint, loaded directly with no community conversion in between, and it ships every key — so a missing one can only be a renamed key upstream (in the port's `sanitize`, or in the repo), and that is an error worth raising at load time rather than a transcript worth debugging later. The revision is pinned even though the repo is org-maintained and loads strictly: strict proves every key is *present*, not that the weights are the ones the constants in this file were measured against, and a retrained checkpoint pushed to main would decode without complaint into different transcripts on the next cold cache. The hash is what makes a transcript reproducible. (The port's `post_load_hook` reads `tokenizer.json` from the snapshot, so a `FileNotFoundError` from there is a broken snapshot, not app code.)
- `_open_container` — the one `av.open` call site for uploads (`mode="r"`, `metadata_errors="replace"`). `av.open` decodes container *and* stream tag text to `str` at open time with `errors="strict"` by default, and FFmpeg's wav demuxer copies RIFF INFO / BWF `bext` text in byte for byte (only ID3 is transcoded) — so an ANSI artist tag from a Windows/DAW tool raised `UnicodeDecodeError` on a perfectly decodable file, and because that is a `ValueError`, `audio_duration_seconds` swallowed it into a silent `None`. Nothing reads a tag, so "replace" costs nothing; one helper rather than a kwarg per call so the two opens cannot drift apart on it
- `_best_audio_stream` — `container.streams.best("audio")` narrowed by `isinstance` to `av.AudioStream | None` (`best()` is typed `Stream | None`). That is `av_find_best_stream` — the track flagged `default`, then the higher-bitrate / more-frames one — i.e. the same pick the torchcodec path made, so a multi-track mp4/mkv keeps transcribing the track it always did. **Not** `streams.audio[0]`: that is the lowest-indexed track, which on a file whose commentary or low-bitrate track comes first is silently the wrong one, with the duration gate reading that track's length too
- `_decode_audio` — PyAV decode of the best audio stream (`_best_audio_stream`) to `(channels, samples)` float32 at the native rate. Every frame goes through an `av.AudioResampler(format="fltp", layout=..., rate=...)` first, built from the **first decoded frame's** layout and rate rather than the stream header's: the two normally agree, but HE-AAC with implicit SBR declares half the rate its frames carry, and a converter targeting the header's rate would have swresample downsample every frame to it and report the wrong rate to the resample that follows (the header only serves the zero-frame return). With layout and rate unchanged the conversion is a pure sample-format one (swresample buffers nothing), so packed and planar codecs, `s16` and `fltp` alike, all concatenate into the same matrix scaled to [-1, 1] — the scaling torchaudio applied, hence bit-exact on 16-bit PCM, and verified identical to the old torchcodec output on both fixtures including the AAC-in-mp4. Raises `ValueError` on a container with no audio stream, and on a track whose codec the bundled FFmpeg cannot decode (PyAV leaves `codec_context` `None` there, and `AudioStream.__getattr__` would otherwise surface it as `'AudioStream' object has no attribute 'layout'`) — both of which the caller turns into the `RuntimeError`
- `load_and_preprocess_audio` — `_decode_audio` on the upload's bytes, downmixes to mono, resamples to `SAMPLE_RATE` (16 kHz) with `torchaudio.functional.resample`; raises `RuntimeError` on failure, including a zero-sample decode and — **after** the resample — anything under `MIN_AUDIO_SAMPLES = 1120` (70 ms at 16 kHz). Both guards belong here rather than downstream: an empty or too-short waveform passes VAD (which falls back to a full-audio segment) and only dies inside the port as a `ValueError` — `audio must contain at least one 10 ms frame` under 160 samples, then an STFT `Input is too short`, a `[reshape]` error from 172, and an opaque `[conv]` shape error from 225 up to 1119 (swept every count from 160 to 1120: 1119 fails, 1120 decodes; the encoder needs 4 stacked mel frames through two stride-2 subsampling blocks and a kernel-7 depthwise conv) — which the UI would surface as an `st.exception` traceback where a `RuntimeError` gets the one-line `st.error`. They are two checks, not one, because the zero check must run *before* the resample (`torchaudio.functional.resample` raises its own opaque reshape `RuntimeError` on an empty tensor at any rate but 16 kHz) while the floor must be measured in the model's samples *after* it (3000 samples at 48 kHz is 1000 here). Segments never trip the floor — VAD buffering makes each at least 0.6 s and `_split_long_segment` parts are ≥ `MIN_SEGMENT_DURATION_S` — so it only ever fires on a whole clip, typically a mis-click on **Record**
- `transcribe_audio` — `model.generate(audio=wav.squeeze().numpy()).text`: one encoder pass and a greedy CTC collapse over a waveform slice. There is no prompt, no token budget and no autoregressive loop — `generate()` takes only the audio (keyword-only `verbose`/`generation_stream`, and `**kwargs` that are **silently ignored**, so a stray `max_tokens=` would be dropped, not rejected) and returns an `STTOutput` whose `.text` is already `.strip()`ped, with `.segments == []` (no word timestamps; the app's timestamps come from VAD) and `.language == "en"`. Output is lowercase and unpunctuated, the way the model's training transcripts were normalised
- `_labeled_toggle` — a settings row: `st.markdown("**label**", help=...)` (bold faux-label, and the help tooltip lives here because a collapsed widget label has nowhere to show one), `st.space("stretch")`, then `st.toggle` with its label collapsed, inside `st.container(horizontal=True, vertical_alignment="center")`. It used to be a `st.columns([15, 1])` pair, which only worked at the centered layout's ~730 px: in the sidebar (300 px by default in 1.64 — user-resizable 200–600 px, remembered per browser in `localStorage["sidebarWidth"]`) a 1/16 column is ~15 px after the padding and gap, narrower than the switch. The stretch space is load-bearing — markdown's default width resolves to `content` inside a horizontal container, so without it the switch sits packed against the label instead of at the sidebar's edge. `TestLabeledToggle` pins the container, the space, the `markdown → space → toggle` order (label → switch → space would pack them together and pass a presence-only check) and that `st.columns` is never called
- `_render_result_card` — takes `(result: PipelineResult, stem: str)` and renders the one result: a bordered `st.container(border=True)` (no `height` kwarg — `height="stretch"` was for equalising cards in a row, and there is one card now; the width cap is the caller's, see `TRANSCRIPT_MAX_WIDTH_PX`) holding a header row — `st.container(horizontal=True, vertical_alignment="center")` with `st.subheader("Transcription")`, `st.space("stretch")` and `st.download_button("", transcript, f"{stem}_transcription.txt", "text/plain", key="dl_txt", icon=":material/download:", help="Download transcription")` floated to the card's right edge — then `st.text(transcript)`. The download is in the header rather than under the text because a 20-minute transcript is ~200 lines, and a button below it was the only action on the page and a few hundred lines below the fold
- `run_pipeline` — `(wav, model, vad_model=None, on_progress=None, use_segmentation=True) -> PipelineResult`: a **single** result, not a dict keyed by task. When segmentation is on it asserts `vad_model` and runs `get_speech_segments`; when off it treats the full audio as one `[0, duration]` segment. Segments only — one `transcribe_audio` per segment, each emitted as `[M:SS - M:SS] text` and joined with newlines into `transcript`. `on_progress` contract, exactly: `(i, total, f"segment {i + 1} of {total}")` **before** each segment, with `total = max(len(segments), 1)`, and nothing else. There is deliberately no closing `(total, total)` call: it existed to hold the bar at full during the guardian pass, and with nothing running after the last segment it would fire microseconds before `main()`'s `finally: progress.empty()`, be coalesced with it in the same flush, and in practice never paint. On the fixture the calls are `[(0, 1, "segment 1 of 1")]`.

  There is no encoder hoist any more, and nothing to replace it with. The 4.1 model was a conformer encoder feeding a 2B LLM decoder, so a segment's encode (~33% of a call, and prompt-independent) could be computed once and shared across N task prompts — `_supports_encoder_hoist` / `_encode_segment` / `_generate_from_features` reached into five `mlx_audio` internals plus `mlx_lm.generate.generate_step` to do that, measured 28% faster on 4 tasks × 5 segments, and carried a name probe, a `try/except` drift fallback and a `RuntimeWarning` to survive upstream renames. With TurboCTC the encode **is** the whole call: one encoder pass, an `argmax`, a CTC collapse and a tokenizer decode, with no prompt, no decoder loop, and only ever one output per segment — so there is nothing to amortise one encode across. The speech model is now driven purely through `mlx_audio`'s public `generate()`, `mlx_lm` is not imported at all, and `_supports_mlx_vad` is the only remaining probe over `mlx_audio` internals. Don't reintroduce a hoist: it would buy nothing and reopen a private surface.

### Models

- [Granite Speech 5.0 470M TurboCTC](https://huggingface.co/ibm-granite/granite-speech-5.0-470m-turboctc) — English transcription (MLX, bf16). **IBM's own repo**, loaded directly by `mlx_audio`'s `granite_speech5_ctc` port with no community conversion in between, pinned to `2864561` (`MODEL_REVISION = "286456107c8ba1161f5c22dfe85466402c88333b"`). Encoder-only: 16 conformer blocks with 128-frame block-local attention (relative positions only within a block), pyramidal 8× temporal subsampling from the 100 Hz mel frames to 12.5 Hz (2× frame stacking, then stride-2 convolutions in blocks 0 and 1), self-conditioned CTC at the midpoint (after block 8 of 16, tied to the final CTC head), and a 16,384-unit BPE vocabulary; greedy CTC decoding, no prompt, no decoder. Apache 2.0. English only; output is lowercase and unpunctuated. Paper: [arXiv 2609.20104](https://arxiv.org/abs/2609.20104), "Design of the IBM Granite 5.0 TurboCTC ASR Model". **Note the `-nc` sibling** (`granite-speech-5.0-470m-turboctc-nc`) is CC-BY-NC-SA 4.0, research and non-commercial only, and is *not* the one loaded. Replaced the `divydeep/granite-speech-4.1-2b-mlx-8bit` community conversion of the 2B encoder+LLM model, which is what carried translation, source languages and keyword biasing
- [Silero VAD v6 (MLX)](https://huggingface.co/mlx-community/silero-vad-v6) — Voice Activity Detection, default backend (pinned to `2ebf4a5`). An mlx-community port of the v6 line; its 16 kHz weights are **bit-exact** with the `silero-vad` PyPI 6.2.1 checkpoint (verified tensor by tensor after the `[O,I,K] -> [O,K,I]` transpose and `bias_ih + bias_hh` fuse), so the two backends agree by construction. Ships 16 kHz only — no 8 kHz branch
- [Silero VAD](https://github.com/snakers4/silero-vad) — the PyTorch build of the same checkpoint, kept as the fallback path (runs on CPU). Note `mlx-community/silero-vad` (no suffix) is the older **v5** line, a genuinely different checkpoint, not a variant of this one

### Languages

English transcription only. Granite Speech 5.0 TurboCTC is an English ASR model with no prompt and no decoder, so there is no source-language selector, no translation and no keyword biasing, and the output carries no casing or punctuation (its training transcripts were normalised to lowercase, unpunctuated text; the app does not restore either).

Known model behaviour — the measurements that set the segment cap and floor (this machine, mlx-audio 0.5.4, greedy CTC on the pinned revision):

- **No length cliff.** Attention is block-local — 128 frames per block at 12.5 Hz (10.24 s), relative positions only within a block — so there is no context length to run out of. One hour of tiled speech transcribed in a single pass with no drift: 357 of 360 repetitions detected, the misses at the artificial tile seams. The old model's "past ~20s it echoes the source language" cliff was an LLM-decoder behaviour and is gone with it.
- **Memory is linear** in clip length, ≈2 MB per second of audio over the 0.88 GB resident base (fresh process per run): 10s → 1.13 GB, 30s → 1.45, 60s → 1.66, 120s → 1.80, 300s → 2.02, 600s → 2.50, 1200s → 3.43, 1800s → 4.53, 3600s → 7.83 GB. That series is MLX alone, and it is *not* what sets `MAX_VAD_OFF_DURATION_S = 1800` on its own: the Streamlit process also holds the PyAV decode at the **source** rate and channel count (a 48 kHz stereo hour is ~1.4 GB float32 before the mono and 16 kHz copies), the upload bytes and the torch baseline — so a single-pass hour lands past 10 GB in-process and swaps a 16 GB Mac, where 30 minutes keeps the whole process around 7 GB. Both process figures include the since-removed guardian (125M fp32 parameters, ~500 MB — the `transformers` import itself stays: `mlx_audio.stt.models` imports the `granite_speech_nar` port, which imports `AutoTokenizer` at module scope, so every speech-model load pulls it in), so they err high — the safe direction for a ceiling. With VAD on there is no ceiling at all.
- **Forced splits cost a little.** WER with equal-length parts and mid-utterance cuts: 73 LibriSpeech dev-clean utterances (1150 words) — **whole 6.17%**, 10 s parts 6.52%, 5 s parts 8.17%; longer utterances had *lower* WER (15–20 s bucket 2.6%), and hypothesis/reference word counts matched. 64 s of continuous TTS speech (198 words) — **whole 8.59%**, 60 s cap 9.09%, 30 s cap 9.09%, 15 s cap 10.10%, 8 s cap 8.59%; through the app's own VAD path, 30 s cap 9.60% (3 segments) vs 8 s cap 10.61% (10 segments). So the cap exists for timestamp readability, not accuracy, and every split is a small cost — hence `MAX_SEGMENT_DURATION_S = 30.0` (about three 128-frame attention blocks, so a forced part never sees less than a full block of context) and `MIN_SEGMENT_DURATION_S = 20.0` (above half the cap, so a span only modestly over it stays whole rather than halving; see `_split_long_segment`). With VAD **off** no cap applies and the whole clip is one inference, which this model handles up to the memory ceiling above — that path just loses the timestamps.

Fixture quirk worth knowing before touching the real-weights test: `tests/data/audio/sample_10s.wav` opens with 0.76 s of exact digital-zero silence. On the **whole clip** (VAD off) the model returns `shapeare on scenery by oscar wilde public` — it drops the second sentence ("this is a librivox recording all librivox recordings are in the public domain"). Through the app's **VAD path**, which trims the lead-in, it returns `shakespeare on scenery by oscar wilde a libvox all librivox recordings are in the public domain` as one segment `[0:00 - 0:09]` (buffered 0.76–9.99 s). The reference `transformers` implementation (5.17.0, which the lock now resolves transitively via `mlx-audio` — nothing here imports `transformers` directly — and which ships the `GraniteSpeech5*` classes; the comparison was originally run against a separate 5.17.0 install while the lock still held 5.15.0, which had none) produces byte-identical output to the MLX port on every clip tried, this one included, so it is the model's behaviour, not the port's; on normal audio (LibriSpeech) word counts match, so it is fixture-specific rather than a general dropped-sentence problem.

### UI Layout

The sidebar holds settings and app info — the VAD toggle and the model name — and nothing a run depends on; the whole flow, input → source → output, is in the main area, in a two-column grid that is the same in every state. That is the bundled Streamlit guidance (`layouts.md`: sidebar for filters, settings and app info; main content stays in main), and it is also what keeps every control on screen when the sidebar is collapsed. `test_sidebar_holds_settings_and_app_info_only` pins the split. Rendered and checked at 1920 px; the narrow cases — the sidebar becomes a collapsible overlay at ≤768 px (the frontend's `md` breakpoint) and `st.columns` stacks at ≤640 px — are Streamlit's documented behaviour and were not rendered. `test_page_config_is_wide` pins the page config by patching `streamlit.set_page_config`, since `AppTest` keeps deltas only and drops the `PageConfig` message.

- **Page config** — `st.set_page_config` sets `page_title`, `page_icon` (`:material/graphic_eq:`) and `layout="wide"`. `initial_sidebar_state` is left at its default (`"auto"`): it was `"expanded"` while the sidebar held the controls, and with only a setting and a caption there the auto-collapse in a narrow window loses nothing

**Sidebar** (300 px by default; the user can drag it 200–600 px and the browser remembers it):

- **VAD segmentation** — `_labeled_toggle` (bold faux-label left, `st.toggle` defaulting to `True` floated right by a stretch space). Help text: "Splits audio into speech segments with timestamps using Silero VAD. Disable to process the whole audio in one pass." (Not "without timestamps": the single full-clip segment goes through the same line formatter, so the transcript still opens with `[0:00 - M:SS]`.) When off, VAD model load is skipped, `run_pipeline` treats the full audio as a single segment, and `MAX_SEGMENT_DURATION_S` does not apply — there is no length cliff to fall off (see Languages), but the single pass is not accuracy-neutral either: on the bundled sample its leading digital-zero silence makes the whole-clip pass drop a sentence that the VAD path recovers, and forced splits cost a little the other way. The trade is timestamps and pause-aligned segments against one uninterrupted context. Part of `_last_input_key` so toggling invalidates cached results. When VAD is off and the audio is longer than `MAX_VAD_OFF_DURATION_S` (1800 s), an `st.warning` is rendered **in the input column, directly above the Transcribe button it disables** (not under the sidebar toggle that clears it: the sidebar can be collapsed, and a disabled button with its reason out of sight is a dead end; the message names the toggle to flip) — `f"Enable VAD segmentation: audio is longer than {MAX_VAD_OFF_DURATION_S // 60} minutes. A single inference that long needs more than 4 GB of memory on top of the decoded audio."`, `icon=":material/warning:"` — the Run button is disabled, and the transcript slot stays empty rather than saying "press Transcribe" beside a disabled button (the only caption in the input column is then the filename). The binding constraint is **memory, not context**: attention is block-local, so there is no context to run out of, and peak MLX memory is linear at ~2 MB per second of audio — 4.53 GB at 30 minutes, 7.83 GB at an hour — and MLX is not the whole process: the decoded audio and the torch baseline sit alongside it, which is what rules a single-pass hour out on 16 GB (the full series and the accounting are under Languages; the old 120 s limit was calibrated for the 2B decoder, which peaked at 14.5 GB there). There is no second, translation-passthrough warning any more: the model has no translation to pass through
- **Model** — `st.caption(f"Model: [{MODEL_ID}](https://huggingface.co/{MODEL_ID}) · English only")`, the model card link that used to be the description under the title. A caption, per `design.md` (app info in the sidebar is caption-weight); it wraps onto two lines at the default width, which is fine for a caption. The "English only" tail is the one language cue on screen, and it is load-bearing: the model transcribes any clip into confident lowercase English-shaped text, so without the cue a wrong-language upload fails silently — `test_sidebar_caption_names_the_loaded_model` asserts it

**Main** (`layout="wide"`):

- **Title** — left-aligned `st.title("Granite Speech Studio")`, and nothing else above the grid: the old one-line description ("Transcribe English audio and video files with the … model") is gone, its model link moved to the sidebar caption. It stays in main rather than the sidebar, where it would wrap onto two lines. (No `text_alignment="center"`: centred text over a two-column grid reads as misaligned)
- **Grid** — `input_col, transcript_col = st.columns([2, 3], gap="medium")`, rendered in every state so nothing moves when a file lands or a transcript appears. **Input column** left, top to bottom: `st.tabs` with Upload (`st.file_uploader`) first, then Record (`st.audio_input`), labels hidden via `label_visibility="collapsed"` (the uploader carries no `help=`: with the label collapsed there is nothing for a tooltip to sit beside, and the dropzone lists the accepted extensions itself); then, once audio is loaded, the preview — `st.video` for video containers, `st.audio` otherwise, selected via `is_video(filename)` — with `st.caption` showing the filename or "Recorded audio"; then the >30-minute warning when it applies; then the **Run button**, `st.button("Transcribe", key="transcribe", type="primary", icon=":material/transcribe:", width="stretch")`, full column width, `can_run = audio_file is not None and not vad_off_too_long` — disabled until audio is loaded, and while VAD is off on audio over 30 minutes (there is no task list to gate on; the key is what the UI tests click). The **transcript slot** right, `transcript_col.container(width=TRANSCRIPT_MAX_WIDTH_PX)`, is the one place everything that answers "where is my transcript?" lands: the empty-state line, the next-step caption, the progress bar and spinners during a run, an error, and finally the card. Input and source left, output right, so the user can scrub the audio while reading
- **`TRANSCRIPT_MAX_WIDTH_PX = 720`** — a cap, not a width: `st.container(width=int)` clamps to its parent. With the sidebar open on a 1440 px laptop the transcript column is ~580 px and the cap does nothing; at 1920 px with the sidebar collapsed the column is 1040 px and the card stops at 720 (measured: card 720, text 688, ~90 characters per line of Source Sans 16 px — the same reading measure as the ~730 px centered column this replaced). The surplus goes to the input column, where a larger video and a longer audio scrubber are worth having; an uncapped card would run past 130 characters a line on a 27" monitor. The cap lives in `main()`, not in `_render_result_card`, whose `st.container(border=True)` call is pinned
- **Slot captions** — no audio: `st.caption("Upload or record audio to get started.")`; loaded but not transcribed: `st.caption("Press Transcribe. The transcript appears here, one timestamped line per segment.")`, shown only when `can_run` and there is no result. Captions rather than alerts because they sit where the card will appear, and no alert-box empty state at all: the uploader itself is the call to action. Nothing while Run is blocked (the warning beside the button already says why)
- **Results** — the single `PipelineResult` and the download stem are captured at run time in `st.session_state["result"]` and `st.session_state["result_stem"]` (the upload's `Path(name).stem`, or `recording_%Y%m%d_%H%M%S` in local wall-clock for a microphone recording), and rendered by `_render_result_card` in the transcript slot as one bordered `st.container(border=True)`. No `height="stretch"` and no per-task grid — both existed to compare per-task cards side by side; the `[2, 3]` columns are input-vs-transcript, not card-vs-card. The card is in page flow, so a long transcript scrolls the player off the top; a fixed-height scrolling slot was considered and rejected (a pixel guess that wastes a tall monitor and captures the scroll wheel — if wanted, it goes on the slot in `main()`, never on the card's pinned container). Both keys are popped whenever `_last_input_key` (`(file_id, use_segmentation)`) changes. `file_id`, not `(name, size)`: Streamlit mints one per upload or recording and keeps it across reruns, so a re-exported file or a second recording of the same length is a new input and drops the stale card, where the `(name, size)` proxy was a hit and left the previous transcript on screen
- **Run feedback** — during a run, inside the transcript slot (so the bar appears where the card will), an `st.progress` bar (`"Starting pipeline..."` → `"Processing: {label}..."`, where the label is `segment i of n`) with per-model `st.spinner`s ("Loading speech model...", "Loading VAD model..."), then `st.toast("Pipeline complete!")` on success. The audio is decoded **before** the speech model loads: every decode-time `RuntimeError` (unreadable file, zero samples, under the 70 ms floor) then surfaces at once, instead of after a ~0.95 GB Hub fetch on a cold cache. `progress.empty()` lives in a `finally` so the error paths clear it too — otherwise a half-filled bar labelled with the failing segment stays on screen beside the error. (`on_progress` fires *before* each unit of work, so the label names what is in flight rather than what just finished; the bar therefore never reaches full — it is emptied, not completed, and a closing call would be coalesced with the `empty()` and in practice not painted — see `run_pipeline` above.)

### Audio Formats

Audio: wav, flac, m4a, mp3, ogg, aac. Video containers (audio track extracted by PyAV): mp4, mov, webm, mkv. `SUPPORTED_FORMATS` is the combined accepted-extension list passed to `st.file_uploader`; `VIDEO_FORMATS` is its video subset, driving conditional preview (`st.video` vs `st.audio`). Upload size limit raised to 500 MB in `.streamlit/config.toml`.

### Performance

- Speech model runs via MLX on Apple Silicon GPU (bf16, 0.88 GB resident). ~380–480× realtime after warm-up on M-series: 20 min of audio in 2.55 s, 1 h in 10.2 s. Peak MLX memory is linear in clip length at ≈2 MB per second of audio — 1.13 GB at 10 s, 1.66 GB at 60 s, 3.43 GB at 20 min, 7.83 GB at 1 h (full series under Languages) — so a 30 s segment under VAD peaks around 1.45 GB
- One encoder pass per segment is the entire inference cost — there is no decoder loop and nothing to hoist (see `run_pipeline`)
- Deferred model loading — speech and VAD models load on first pipeline run, not on page load
- `@st.cache_resource` to cache models
- No torch decorator on `run_pipeline` — the PyTorch VAD fallback's `get_speech_timestamps` is `@torch.no_grad()` itself, and nothing else in the pipeline touches autograd
- `io.BytesIO` for in-memory audio loading (no temp files)
- Audio downmixed to mono and resampled to `SAMPLE_RATE = 16000` Hz on load
- `audio_duration_seconds` cached in a single `st.session_state["_duration"]` slot holding `(file_id, duration)` so the upload buffer isn't re-copied on every rerun (matters for 500 MB uploads); the slot is overwritten when the file changes, so it can't grow unbounded (no eviction needed)
- Silero VAD runs on the Apple GPU via MLX (~1.2 MB of weights, 309K params); encoder batched across chunks then one LSTM pass, ~3x faster than the PyTorch path and identical in output. `VAD_ENCODER_BATCH_CHUNKS = 2048` bounds peak memory (+43 MB over streaming on 15 min of audio). Falls back to PyTorch CPU on any drift

### Error Handling

- `RuntimeError` caught explicitly for transcription failures
- Unexpected exceptions shown with `st.exception()`

### Downloads

- **Text** — the one transcript as `<stem>_transcription.txt` (`text/plain`), icon-only download button (`:material/download:`, `key="dl_txt"`) with tooltip "Download transcription". The stem is the upload's filename stem, or `recording_<timestamp>` for a microphone recording

### Tests

`tests/test_streamlit_app.py` — unit tests over the helpers, model loaders, audio loading, transcription, pipeline execution and result-card rendering, with `st` mocked wholesale. `grep "def test_"` is the coverage inventory, so what follows is only the conventions and the invariants worth not breaking. Pure helpers are module-level `pytest.mark.parametrize`d functions; anything stateful is a `Test*` class. Two assertions are load-bearing rather than incidental: **segments never overlap and stay monotonic across cap values** when the cap refuses a merge, and `load_vad_model`'s two fallbacks (repo unloadable, internals moved) each assert the `RuntimeWarning` — a silent downgrade is the failure mode both paths exist to prevent. `TestGetSpeechSegments._run` takes an explicit `max_segment_duration` so cap tests don't depend on the shipped constant's value; the shipped values are pinned separately — `test_default_cap_and_floor` asserts `MAX_SEGMENT_DURATION_S == 30.0` and `cap / 2 < MIN_SEGMENT_DURATION_S <= cap`, and two `_split_long_segment` cases at the default cap pin the consequence (30.1s stays whole; 45s splits into 2 × 22.5s, not 30 + 15). `test_model_revision_is_pinned` demands a full 40-hex hash, since a tag or branch is a valid `revision` that can still move. Shared `pipeline_mocks` fixture supplies the two positional arguments after `wav` in `run_pipeline`'s signature — `(model, vad_model)`, in that order — via a `NamedTuple` so tests can unpack with `*pipeline_mocks`; the speech model is `MagicMock(spec=["generate"])` with `generate.return_value.text` set, because a bare `MagicMock` would conjure any other attribute the code reached for rather than fail. `TestTranscribeAudio` pins the `generate` kwargs set to exactly `{"audio"}`: the real `generate()` has `**kwargs`, so a stray `prompt=` or `max_tokens=` would be swallowed silently, and the test is the only thing that would object. `TestLoadModel.test_loads_strictly` asserts `strict` arrives **by keyword** (`call_args.kwargs["strict"] is True`) — `mlx_audio`'s signature is `(model_path, lazy=False, strict=False, **kwargs)`, so a positional `True` would land on `lazy` and leave `strict` at its default. `TestRunPipeline` patches `get_speech_segments` via an autouse `pytest.fixture` with a default single 9s segment, and overrides it per-test with a two-segment fixture for multi-segment cases. It pins the single-`PipelineResult` contract: the exact key set is the `TypedDict`'s, `{"transcript"}` (no dict keyed by task), `[0:00 - 0:09] decoded text` line format with multi-segment lines newline-joined, one `generate` call per segment on that segment's slice, `use_segmentation=True` asserting on a missing `vad_model` and `False` never touching VAD, and the exact `on_progress` sequence, parametrized over both segment fixtures — `[(0, 1, "segment 1 of 1")]` on the default single segment (what every VAD-off run and the bundled fixture emit) and `[(0, 2, "segment 1 of 2"), (1, 2, "segment 2 of 2")]` on the two-segment one — with no closing `(total, total)` entry in either. The `@st.cache_resource` wrappers are bypassed via module-level `_load_model` and `_load_vad_model` aliases pointing at `.__wrapped__`; `run_pipeline` is undecorated and called directly. `TestRenderResultCard` asserts the container calls are exactly `[call(border=True), call(horizontal=True, vertical_alignment="center")]` — the bordered card, then the header row — with **no** `height` kwarg on either, one `st.space("stretch")`, the full `mock_calls` name sequence — card `__enter__`, row `__enter__`, `subheader`, `space`, `download_button`, row `__exit__`, `text` — because a filtered order check over three names passes with the button emitted after the row closes, or the space emitted after the button (verified by mutation), the download button's positional args `("", transcript, "<stem>_transcription.txt", "text/plain")`, and that neither `st.success` nor `st.warning` is ever called — the card has no status banner, and that assertion is the guard against one coming back. Test fixtures in `tests/data/audio/` (`sample_10s.wav`, `sample_10s_video.mp4`).

`TestGraniteSpeech5Contract` exercises the genuinely installed `mlx_audio` surface the speech path relies on, since everywhere else the model is a `spec=["generate"]` mock and an upstream change would ship green. The app reaches into no private speech-model internals any more, so this is the whole contract, one registry check, two signature checks and one smoke test: `"granite_speech5_ctc"` is in `mlx_audio.stt.utils.MODEL_REMAPPING` (what the `>=0.5.1` floor in `pyproject.toml` promises — absent in 0.5.0 and 0.4.x, where the load fails with an unsupported-model error rather than an `ImportError`); the port's `Model.generate` has an `audio` parameter *and* a `**kwargs` (which is why `TestTranscribeAudio` pins the kwargs set — the model would never object to a stray one); and `mlx_audio.stt.utils.load_model` has `strict` as a **named** parameter rather than something that would fall into its `**kwargs` and be forwarded or dropped silently, quietly restoring the non-strict default. `test_transcribes_the_fixture_with_real_weights` then loads `MODEL_ID` at `MODEL_REVISION` through `_load_model`, runs `sample_10s.wav` through `load_and_preprocess_audio` and `transcribe_audio` — the whole clip, no VAD — and asserts only that the text is non-empty, equals its own `.lower()`, and contains `oscar wilde`. It runs **only where the pinned snapshot is already in the local HF cache** (gated on `huggingface_hub.try_to_load_from_cache(MODEL_ID, "model.safetensors", revision=MODEL_REVISION)` returning a path, which with a full commit hash never touches the network) and `pytest.skip`s otherwise — CI included. That gate is not the try/except-skip pattern `test_both_backends_agree_on_real_audio` uses, on purpose: `mlx_audio`'s loader calls `snapshot_download` on a cache miss and does *not* raise, so on a networked runner the load would *succeed* by fetching 0.95 GB per matrix leg per run. The try/except around the load stays for the other case, a cached snapshot that no longer loads. **Do not tighten that assertion to the full sentence.** On the whole clip the model returns `shapeare on scenery by oscar wilde public` — the second sentence dropped, the first word misread — and the reference `transformers` implementation returns the byte-identical string, so this is the model's behaviour on this fixture (the digital-zero lead-in, see Languages), not the port's. Pinning the exact text would pin a quirk: a retrained checkpoint that *fixes* it would then fail the test for getting the transcript right, and the VAD path already yields a different string from the same clip. Greedy CTC on a pinned revision is deterministic, so the assertions are stable; `oscar wilde` is what every path — whole clip, VAD path, reference implementation — agrees on, and lowercase is the one output property the model guarantees.

`TestMlxVadInternals` is the VAD equivalent, and exists for the same reason: everywhere else `_mlx_vad_probabilities` is mocked, so without it the code driving `mlx_audio`'s branch submodules never runs in CI. It builds a **real** `mlx_audio` `Model` from a real `ModelConfig` with random weights — no download, but real classes, real shapes, and a real streaming implementation (`_predict_proba_array`) to check the batched one against. Two tests assert that equivalence: one at the default batch size, and one with `VAD_ENCODER_BATCH_CHUNKS` patched to 4, which is the only way the `(hidden, cell)` handoff across batches gets exercised — drop the carry and a single-batch test still passes. A third builds a `Model` with `hop_length=64` and asserts `_mlx_vad_probabilities` **raises** rather than dropping frames. `test_both_backends_agree_on_real_audio` then runs real weights on `sample_10s.wav` through both backends and demands identical spans; it `pytest.skip`s (not fails) when the repo cannot be fetched, so an offline CI run does not go red over a network absence. Pinning `MLX_VAD_REVISION` froze one side of that pair and `silero-vad==6.2.1` freezes the other (see Dependencies), so while both pins stand the test is a tripwire for the pins themselves; it becomes load-bearing again the moment the silero-vad pin is lifted, when a `uv lock` could bump it to a future Silero release and leave the backends disagreeing with nothing else to notice.

`make_mlx_vad_model(drop=...)` builds a spec'd stand-in for the MLX model with any one probed name removed — `spec=` throughout, since a bare `MagicMock` conjures every attribute the probe asks for and would never report drift. `TestSupportsMlxVad` parametrizes it over all 19 probed names, plus two cases asserting a mismatched `chunk_size`/`context_size` is refused by value; `TestSileroVadDispatch` covers the four routing outcomes (torch model → torch, MLX model → batched, non-16 kHz → torch regardless, MLX raising → warn + torch). `TestSileroVad` now passes `MagicMock(spec=torch.nn.Module)`, since a bare mock no longer routes to the PyTorch path.

`tests/test_app_ui.py` — end-to-end UI tests driving the real `main()` widget tree via `streamlit.testing.v1.AppTest` (complements `test_streamlit_app.py`, which mocks `st` entirely and never exercises the UI wiring). Covers default widget state (title, the one toggle on and nothing else in the tree — `segmented_control`, `pills` and `multiselect` all asserted absent — Run disabled, no subheader, no `result` in session state), the page config (`layout="wide"`, no `initial_sidebar_state`, observed by patching `streamlit.set_page_config`), the sidebar/main split (`test_sidebar_holds_settings_and_app_info_only`: the toggle and one caption in `at.sidebar` and nothing else; uploader, recorder and the one "Transcribe" button in `at.main`, the title in main only; `audio_input` has no typed accessor, so it goes through `Block.get("audio_input")`), the empty state (`test_empty_state_keeps_the_grid`: both columns already present, the uploader and a disabled Run in the input column, no filename caption, the capped slot in the transcript column holding one "Upload or record" caption, no `st.info` anywhere), the sidebar caption linking `MODEL_ID`'s model card and saying "English", with main pinned by tree shape — `title, grid = at.main.children.values()` — so a description put back under the title as *any* element type fails the unpack (a markdown count would miss a caption, and a caption between the title and the grid sits outside both columns), the bold VAD faux-label in the sidebar (with `**Toxicity check**` and `**Keywords**` both asserted gone from the *whole* tree, not just the sidebar — a faux-label creeping back beside the uploader is what those guards exist to catch), Run enabling as soon as audio is present (there is no task selection to wait on) together with the loaded-state grid — split as `input_col, transcript = at.columns`: the Transcribe button, the player (`input_col.get("audio")`, since `st.audio` has no typed accessor either) and the filename caption in `input_col`, none in the sidebar, and in `transcript` exactly one container whose `proto.width_config.pixel_width == TRANSCRIPT_MAX_WIDTH_PX` (the only place the cap is observed, since the card test pins that the card carries no width) holding the "Press Transcribe" caption — the VAD-off long-audio gate parametrized at 1801 s (blocked: one warning in the input column containing "longer than 30 minutes" and "more than 4 GB", `:material/warning:` icon, none in the sidebar, and no caption in the transcript column) and 1799 s (no warning, one caption in the transcript column), with the filename caption in the input column either way, a 30 s VAD-off clip rendering **no** warning at all — the prompted model fired its translation-passthrough warning there, and the 30-minute memory gate is the only VAD-off warning left — the single-slot `_duration` cache (a hit across reruns, a miss — and overwritten, not accumulated — on an identical re-upload; hit and miss are told apart by counting `av.open` calls through a `wraps=` patch, since a recompute under the same `file_id` stores the same key), the `config.toml` guards, and end-to-end Runs: VAD on (one timestamped line, and **no** `st.success` or `st.warning` element anywhere in the tree — the card carries no status banner of any kind), VAD off (no VAD load, one `generate` over the full clip's sample count, one timestamped line), flipping the toggle after a run dropping both `result` and `result_stem`, and re-uploading the same-named, same-sized file after a run dropping them too (the `file_id` regression guard: it fails on the `(name, size)` key). Every Run asserts the same single card: one subheader "Transcription", one `st.text`, one download button with `key="dl_txt"`, the help text and the icon, `result_stem == "sample_10s"`, no `st.info`, and — by column — the transcript column's three containers picked by property — the slot by `proto.width_config.pixel_width == TRANSCRIPT_MAX_WIDTH_PX`, the card by `proto.flex_container.border`, the header row by `direction == Block.FlexContainer.HORIZONTAL` — rather than unpacked by count, so a Streamlit release that renders a spinner or an emptied progress bar as a block fails a named assertion instead of a `ValueError` (Block accessors recurse, which is why all three are visible from the column): the subheader and download button in the header row, the text in the card, no caption left in the slot, and the input column holding the filename caption and no card parts (a card rendered under `input_col` would pass a whole-tree count) — the download *filename* is hashed into the served media URL rather than carried on the element, so it is pinned by the unit-level card test instead. Layout assertions go through `at.sidebar.*`, `at.main.*` and the two `at.columns`, and Run is clicked by key: `AppTest` walks main (root child 0) before the sidebar (child 1) and the input column before the transcript column, so `at.button[0]` and `at.caption[0]` would keep resolving to the right elements only until something else rendered one above them. Durations are faked through the `_header_duration` helper, which patches the shared `av.open` with a stream carrying `duration` in a `Fraction(1, 16000)` time base — so the app's `duration * time_base` arithmetic runs rather than being bypassed — since no fixture is half an hour long; the Run tests bound the VAD segment against the fixture's frame count read with the stdlib `wave` module, not with the decoder under test. AppTest re-executes `streamlit_app.py` in a fresh namespace on every `.run()`, so mocks must patch the **shared upstream imports** (`mlx_audio.stt.utils.load_model` for inference, `mlx_audio.vad.utils.load_model` for VAD, `av.open` for header-only duration reads) rather than `streamlit_app.*` — patching `streamlit_app` attributes does not cross AppTest's script-runner boundary, and clicking Run without an upstream patch would fetch the real ~0.9 GB speech model and the MLX VAD checkpoint from the Hub. Session-state assertions go through the 1.64.0 `_AppTestSessionState` wrapper (streamlit#16897): `in`, iteration and the dict methods serve `filtered_state` — user keys plus keyed widgets; a leading underscore such as `_duration` is *not* internal, only the `$$STREAMLIT_INTERNAL_KEY` prefix is — while `[]`, attribute access and `get()` reach the raw state. There is no `.filtered_state` attribute on it any more (it falls through `__getattr__` to an `AttributeError`), so membership is spelled `"result" not in at.session_state` and the single-slot check iterates `at.session_state` for every key starting with `_duration` (a per-upload `_duration_<file_id>` key would show up there); that iteration is the one line that needs the `dev` group's `streamlit>=1.64.0` floor. The Run tests are hermetic: the `speech_loader` fixture patches the MLX loader to return a `MagicMock(spec=["generate"])` whose `generate` yields `"hello world"`; `load_model` calls it as `(MODEL_ID, revision=MODEL_REVISION, strict=True)` — every Run pins that with **`assert_called_once_with`** — so the mock must accept `strict=True` by keyword, and a stand-in with a narrower signature would raise inside the script runner and surface as an exception element rather than a card. `_offline_mlx_vad()` makes the MLX VAD loader raise `OSError("offline")` so `load_vad_model` takes its documented fallback onto the PyTorch build inside the `silero-vad` wheel — real VAD spans on the fixture (the one segment is asserted shorter than the clip, since VAD trims the 0.76 s lead-in, and the line still reads `[0:00 - 0:09] hello world`), nothing downloaded, and the `RuntimeWarning` asserted with `pytest.warns(..., match="using the PyTorch VAD")`. An autouse fixture clears `st.cache_resource` before each test, since AppTest does not reset Streamlit's resource cache between instances. Pure-UI cases avoid clicking Run entirely.

The two `config.toml` guards are both written to outlive the config they were added for. `test_config_defines_no_custom_theme` asserts *conditionally* — `if "theme" in app_config` — so re-adding a theme does not make it fail and get deleted; it switches to enforcing the both-sub-palettes rule above, which is the regression it actually exists to catch — by value, not just by header, since an empty `[theme.light]` sets nothing and collapses the menu just the same. `test_config_has_no_invalid_options` checks every key against `config._config_options_template` because Streamlit **drops unknown options silently** instead of rejecting them, so a typo'd `maxUploadSize` would cost 500 MB uploads with nothing on screen to say so. Its `_option_paths` helper is module-level and separately parametrized: the shipped file has one key and exercises one branch, so the shapes that would break it are pinned by synthetic tables instead — an option whose *value* is a table (`server.trustedUserHeaders`) or an array of tables (`[[theme.fontFaces]]`) must terminate the walk, since descending into free-form inner keys fails a valid config, while a typo under an unregistered prefix and an empty typo'd section header must still be reported.

## Resources

- [Granite Speech 5.0 470M TurboCTC](https://huggingface.co/ibm-granite/granite-speech-5.0-470m-turboctc) — the model card, and the repo loaded at runtime
- [Design of the IBM Granite 5.0 TurboCTC ASR Model](https://arxiv.org/abs/2609.20104) — the paper (arXiv 2609.20104)
- [mlx-audio `granite_speech5_ctc`](https://github.com/Blaizzy/mlx-audio/tree/main/mlx_audio/stt/models/granite_speech5_ctc) — the port's README and source
- [Granite Speech Models](https://huggingface.co/collections/ibm-granite/granite-speech)
