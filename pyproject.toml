[project]
name = "granite-speech-pipeline"
version = "0.4.0"
description = "Speech-to-text and translation pipeline with punctuation and toxicity detection using IBM Granite models"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.12.0",
    "punctuators>=0.0.5",
    "silero-vad",
    "streamlit",
    "torch",
    "torchaudio",
    "torchcodec>=0.10.0",
    "transformers",
]

[dependency-groups]
dev = [
    "pytest",
    "ty",
    "ruff",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff.lint.isort]
combine-as-imports = true

[tool.ty.environment]
python-version = "3.12"
